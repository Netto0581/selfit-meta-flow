{
  "name": "selfit-meta-flow",
  "description": "API para o fluxo da Selfit no Meta Flow",
  "env": {
    "PYTHON_VERSION": {
      "description": "Python version",
      "value": "3.9.0"
    },
    "FORCE_HTTPS": {
      "description": "Force HTTPS",
      "value": "true"
    }
  },
  "build": {
    "builder": "NIXPACKS"
  },
  "scripts": {
    "start": "python app.py"
  }
} 